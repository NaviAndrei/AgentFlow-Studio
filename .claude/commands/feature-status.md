Read CLAUDE.md and src/types/index.ts, then answer:
1. List every node type in REGISTERED_NODE_TYPES
2. List every WorkspaceRole and its permissions from ROLE_PERMISSIONS
3. List the 5 new utility files added in the F13–F18 batch and their single-line purpose
4. What is the V2 upgrade path for the code sandbox?
5. What guard prevents Live mode from using the simulated codeExecutor fake?

Answer in a compact bullet list. No code output needed.
