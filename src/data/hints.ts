import type { AgentFlowNodeType } from '../types'

/**
 * All HintIcon tooltip copy, grouped by where it appears. Centralized so the
 * wording can be reviewed/edited without touching component files.
 */
export const HINTS = {
  /** Node palette tiles (Sidebar) — one sentence per node type. */
  nodes: {
    start: 'Where the flow begins. Define the inputs your flow expects, like a user’s message.',
    llm: 'Sends a prompt to an AI model and gets a text response back.',
    agent: 'An AI that can think in steps and use tools to complete a task, not just reply once.',
    tool: 'A function the AI can call, like a calculator or a web search — you define what it does and what data it needs.',
    memory: 'Stores conversation history so the AI can refer back to earlier messages.',
    output: 'The final message shown to the user — the end of the flow.',
    condition: 'Branches the flow down different paths based on a simple if/else check.',
    router: 'Uses the AI to decide which path the flow should take next, based on the content so far.',
    guardrail: 'Checks the AI’s output against a rule before it continues. Like a quality gate — the flow only proceeds if it passes.',
    join: 'Waits for multiple parallel paths to finish, then combines their results into one.',
    loop: 'Repeats a part of the flow until a condition you set becomes true.',
    humanInLoop: 'Pauses the flow and waits for a person to approve or reject before continuing.',
    supervisor: 'An AI manager that assigns tasks to other agents and combines their results.',
    swarmWorker: 'One of a team of agents that can hand off the conversation to each other as needed.',
    retriever: 'Searches a knowledge base for the most relevant snippets to feed to the AI, based on meaning rather than exact words.',
    mcpServer: 'An external tool server your agent can call. Lets the agent use capabilities beyond text — like searching files, running code, or calling APIs.',
    longTermStore: 'Memory that persists across separate conversations, organized into named folders (namespaces).',
    memoryWriter: 'Automatically pulls useful facts out of a conversation and saves them for later.',
    multimodalInput: 'An entry point for non-text input, like an image, audio clip, or document.',
    computerUse: 'Lets the AI control a computer screen directly — taking screenshots and clicking/typing.',
    subgraph: 'Embeds another saved flow as a single step, so you can reuse it as a building block.',
    codeExecutor: 'Runs code generated by the AI in a safe, isolated environment, then returns the result.',
    structuredOutput: 'Forces the AI’s response into a specific data shape (like a form with required fields) instead of free-form text.',
    evaluator: 'Has the AI grade a result (pass/fail, a score, or a letter grade) and route accordingly.',
    map: 'Runs the same steps separately for each item in a list, in parallel.',
    planner: 'Has the AI break a big goal down into a list of smaller tasks.',
    subagent: 'A helper AI that works on a task with its own separate memory, then reports back.',
    a2aAgent: 'Calls another AI agent running on a different server, using a shared agent-to-agent protocol.',
    tryCatch: 'Watches the branch after it for errors. If something fails, the flow reroutes to the error path instead of stopping.',
    retry: 'Automatically re-runs the next step if it fails or returns nothing, waiting longer between each attempt.',
    httpRequest: 'Calls an external API directly — GET, POST, PUT, DELETE, or PATCH. Use it to fetch data or send updates to any HTTP service.',
    note: 'A free-text comment on the canvas — doesn’t affect how the flow runs.',
    group: 'A frame you can use to visually organize and collapse a set of nodes.',
  } satisfies Record<AgentFlowNodeType, string>,

  /** Inspector config field labels — jargon-heavy fields only. */
  fields: {
    systemPrompt: 'A private instruction given to the AI before the conversation starts — it sets the AI’s role and rules.',
    temperature: 'Controls how creative or predictable the AI’s responses are. Lower = more consistent, higher = more varied and sometimes surprising.',
    maxIterations: 'The most times the agent can loop between thinking and using a tool before it has to stop and respond.',
    toolSchema: 'Describes the shape of the data this tool expects (or returns), so the AI knows how to call it correctly.',
    memoryType: 'short-term keeps recent messages; vector-store enables semantic search over past content; checkpointer saves the whole conversation state so it can be resumed later.',
    routingPrompt: 'Tells the AI how to decide which route to pick — describe the categories it should choose between.',
    checkType: 'keyword checks for required words in the response. llm-judge asks an AI to judge whether the response meets a rule you describe.',
    criteria: 'The words that must appear for a ‘keyword’ check, or the description of what a passing response looks like for an ‘llm-judge’ check.',
    mergeStrategy: 'concat combines the output from every branch into one. last keeps only the result from whichever branch finished last.',
    loopCondition: 'A condition checked after each pass — once it’s true, the flow exits the loop and moves on.',
    allowNetworkAccess: 'Lets the executed code make network requests (like calling an API). Off by default for safety.',
    inputVariable: 'The name used to refer to this file/image/audio input elsewhere in the flow.',
    encoding: 'How the file is provided to the AI: a direct link (url) or the file’s raw data embedded as text (base64).',
    agentUrl: 'The address of the remote agent’s server — this is where the request is sent. Use the full URL including /a2a.',
    authToken: 'A secret credential sent with the request to prove your agent is allowed to call this remote agent.',
    maxSteps: 'The most screenshot → action cycles the AI can take before it must stop.',
    allowedTools: 'Which on-screen actions the AI is permitted to perform during this task.',
    decompositionPrompt: 'Tells the AI how to break the overall goal into a checklist of smaller tasks.',
    taskInput: 'The name of the input value this subagent receives as its task.',
    namespace: 'A named folder for organizing stored memories, so different topics or users don’t mix together.',
    storeOperation: 'read fetches existing memories, write saves new ones, and search (semantic) finds memories related to a query by meaning, not exact words.',
    memoryKind: 'episodic records what happened (events), semantic records facts, and procedural records rules or instructions to remember.',
    extractionPrompt: 'Tells the AI what kind of information to look for and save from the conversation.',
    ioMapping: 'Maps variables from this flow into the embedded flow’s inputs (or its outputs back out), as a small JSON object of name pairs.',
    appendToParent: 'If on, a summary of what the embedded flow did is added to this flow’s conversation history.',
    scoreType: 'pass_fail gives a simple yes/no, numeric gives a score on a scale, and letter_grade gives an A–F style grade.',
    threshold: 'The minimum score needed to count as a ‘pass’ when using a numeric score.',
    mapListSource: 'The name of the list (already in the flow’s data) to split into separate parallel runs, one per item.',
    maxParallel: 'The most items that can be processed at the same time.',
    topK: 'How many matching results to retrieve — a higher number returns more, possibly less relevant, results.',
    similarityThreshold: 'How closely a result must match the query to be included. Higher = stricter, fewer but more relevant results.',
    serverUrl: 'The address of the external tool server this node connects to.',
    pydanticModel: 'The name of the data shape (schema) the AI’s response must follow — defined as a Python class when exported.',
    jsonSchema: 'A blueprint describing exactly which fields the AI’s response must contain and their types.',
  },

  /** Navbar and MetricsBar controls. */
  controls: {
    simulate: 'Runs your flow step-by-step with test data, so you can see how it behaves without using real AI calls.',
    live: 'Switches the simulation to use real AI calls via your configured provider. Uses API credits.',
    exportPython: 'Generates runnable Python (LangGraph) code for this flow, so you can deploy it outside the app.',
    stepCounter: 'Shows which step of the simulation is currently running, out of the total planned steps.',
    activeCount: 'How many nodes are currently running at the same time (e.g. inside a Map or parallel branch).',
    tokens: 'An estimate of how much text the AI has processed so far. Tokens are the small chunks of text AI models are billed by.',
    step: 'Runs just the next node in the simulation, then pauses again.',
    trace: 'Opens a log of every step the simulation has run, with inputs, outputs, and timing.',
    approve: 'Continue the flow — the Human-in-Loop step passed.',
    reject: 'Stop here and skip all remaining steps.',
  },

  /** Trace log and State Inspector labels. */
  trace: {
    title: 'A step-by-step record of everything that ran during the simulation, in order.',
    engineLive: 'This node made a real call to an AI provider. API credits were used.',
    engineSimulated: 'This node ran with fake test data, not a real AI call. Safe for testing your flow without using API credits.',
    durationMs: 'How long this step took to run, in milliseconds (1000ms = 1 second).',
    messages: 'The running conversation history that gets passed between nodes as the flow executes.',
    innerSteps: 'This Subgraph node ran its own embedded flow internally — this is how many steps that inner flow took.',
    mapBranches: 'This Map node split into this many parallel copies, one per item in its list.',
  },
} as const
